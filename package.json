{
    "name": "new-portfolio",
    "version": "1.0.0",
    "description": "Portfolio .NET API + React SPA",
    "private": true,
    "scripts": {
        "dev": "npm --prefix frontend-react run dev",
        "build": "npm --prefix frontend-react run build",
        "dev:react": "npm --prefix frontend-react run dev",
        "build:react": "npm --prefix frontend-react run build",
        "start": "dotnet run --project Portfolio.Api --launch-profile http",
        "start:api": "dotnet run --project Portfolio.Api --launch-profile http",
        "test": "echo \"No root tests (add tests in frontend-react as needed)\" && exit 0"
    },
    "author": "Jessica Lecker",
    "license": "ISC"
}
